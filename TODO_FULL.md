# TODO List

- [x] Repo audit & status mapping
  - Map each user requirement to current code/artefacts and produce this todo list (audit performed).
- [x] Verify dev server & pgr routing (realistic routes)
  - Ensure Next.js dev server runs and pgr_dijkstra routes produce real road geometries (not straight lines).
- [ ] Restore AddressInput component & geocoding UI
  - `page.tsx` imports `@/components/AddressInput` but no such component was found. Restore or reimplement `AddressInput` (search box -> geocode -> find_nearest_node RPC) and wire up geocoding to UI. This is essential for requirement 2 alternative entry by address.
- [ ] Verify/complete geolocation behavior
  - `useMyLocation` exists and calls `find_nearest_node` RPC; confirm the RPC exists in DB and that it returns node IDs. If missing, add RPC or implement server API `/api/geocode` to perform reverse/forward geocoding and nearest-node lookup.
- [x] Load and display threat metadata with controls
  - Threat layers are loaded in `page.tsx` (inundaciones, reportes, DGA) and there are checkboxes to toggle them. Validate DB tables exist and geometries render correctly. Add popups for non-polygon fields (done in code).
- [ ] Generate per-node and per-edge failure probabilities (p_fallo files)
  - Implement script to compute `p_fallo_nodo` and `p_fallo_arista` from threat tables (amenaza_*), create/overwrite columns in `infra_nodos` and `infra_aristas` and document assumptions. This is required for resilient routing and simulations.
- [ ] Implement/verify the 4 route techniques
  - Ensure the 4 methods exist and are runnable: (a) pgr_dijkstra distance-only (baseline) — implemented; (b) optimization with CPLEX/GUROBI — not implemented (requires solver license/install); (c) pgr_dijkstra with risk (resilient) — implemented but depends on populated `p_fallo_arista`; (d) metaheuristic/A* — endpoint exists calling Python script, but Python modules (`metaheuristics`) are missing, and the script fails without dependencies. Action: populate p_fallo columns, add/complete metaheuristic Python code, and (optionally) add GUROBI model or use open-source MILP solver (CBC) as alternative.
- [ ] Expose computation times and enable toggling routes in UI
  - APIs return computation_time metrics for baseline and resilient. The comparator endpoint aggregates times. UI offers toggles to show/hide each route. Verify end-to-end for A*/GUROBI once implemented.
- [ ] Random failure simulation & checkbox to show only threats that could occur
  - `/api/simulate-failures` exists and page.tsx includes simulation controls and rendering for simulationEntities. However: example simulation storage `sim_fallas_activas`, `p_fallo_nodo` columns, and a UI checkbox to filter threats that 'could occur' are not fully verified/implemented. Add a 'Show only probable threats' checkbox that filters displayed threat layers to only items flagged by the last simulation.
- [ ] Create reproducible example case demonstrating alternative route
  - `web/public/example_route.json` is present but empty. Create a deterministic example JSON (baseline + resilient + metaheuristic + gurobi if available) showing an alternative route that mitigates threats, with metrics and explanatory text.
- [ ] Install/configure Python environment for A* and GUROBI/CPLEX endpoints
  - Provision the Python environment, add missing modules (metaheuristics, networkx, psycopg2), and decide/implement solver strategy for optimization (install Gurobi if license available or switch to CBC/OR-Tools). Test and make A*/GUROBI endpoints return successfully (currently Python process exits).
- [ ] Add tests and smoke checks (build -> lint -> run example)
  - Add a small test script that starts Next.js in dev mode, pings `/api/route/compare` with example nodes, and asserts FeatureCollections are returned and metrics present. Also add simple unit tests for Python A* module once implemented.
- [x] Document final README and run instructions (update)
  - Update README with steps to: run dev server, reload infra (reload_infraestructura.py), populate probabilities, provision Python env for optimization, and how to run simulations/examples. Include notes about pooler (session mode) and fallback IP.
